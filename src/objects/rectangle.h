#ifndef RECTANGLE_H
#define RECTANGLE_H

struct Rectangle {
    float x;
    float y;
    float width;
    float height;
};

struct Position {
    // NOLINTNEXTLINE(readability-identifier-length)
    float x;
    // NOLINTNEXTLINE(readability-identifier-length)
    float y;
};

#endif // RECTANGLE_H
